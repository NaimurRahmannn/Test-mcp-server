[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "opensteward-test-service"
version = "0.1.0"
description = "Live GitHub fixture for OpenSteward MCP integration tests"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]

